name: Deploy GitBook

on:
  push:
    branches: [ main ]
    paths: [ 'docs/**', '.gitbook.yaml' ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '16'  # Using Node 16 for better GitBook compatibility

      - name: Install and Fix GitBook
        run: |
          # Install GitBook CLI
          npm install -g gitbook-cli@2.3.2
          
          # Fix graceful-fs compatibility issue
          cd $(npm root -g)/gitbook-cli/node_modules/npm/node_modules/
          npm install graceful-fs@latest --save
          
          # Verify GitBook installation
          gitbook --version

      - name: Build GitBook
        run: |
          cd docs
          gitbook install
          gitbook build . ./_book

      - uses: actions/configure-pages@v4

      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
